---
# VPC CIDR allocation - randomised to avoid conflicts
vpc_cidr: 10.{{ 256 | random(seed=resource_prefix) }}.180.0/22
subnet_a_cidr: 10.{{ 256 | random(seed=resource_prefix) }}.181.0/24
subnet_b_cidr: 10.{{ 256 | random(seed=resource_prefix) }}.182.0/24
