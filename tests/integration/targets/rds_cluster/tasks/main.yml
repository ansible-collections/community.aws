---
- module_defaults:
    group/aws:
      region: "{{ aws_region }}"
      aws_access_key: "{{ aws_access_key }}"
      aws_secret_key: "{{ aws_secret_key }}"
      security_token: "{{ security_token | default(omit) }}"
  collections:
    - community.aws
    - amazon.aws

  block:
  #- include_tasks: create_cluster.yml
  
  # Failing
  # - include_tasks: restore_cluster.yml

  # TODO: An error occurred (InvalidParameterValue) when calling the CreateDBCluster operation: Replication from cluster in same region is not supported
  #- include_tasks: promote_cluster.yml

  - include_tasks: create_cluster_vpc_sgs.yml
  #- include_tasks: modify_cluster.yml
  #- include_tasks: tag_cluster.yml




